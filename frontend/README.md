# Foundr Frontend

## Tech Stack
- Next.js (React)
- Tailwind CSS
- GitHub OAuth (via backend)

## Setup
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`

## Features
- GitHub login
- Dashboard (scores, sprints, teams)
- Team formation UI
- Feedback UI
